Wenn dieses Projekt verändert wird, muss ContentProcessors
auch neu kompiliert werden (passiert automatisch).

DANACH werden allerdings alle Content-Files neu verarbeitet
(was dann an die 2 Minuten dauern kann). Deswegen liegen hier
nur reine Datentypen drin.

Alles andere bitte nach KeplersLibrary.
